import React from "react";

const List = ({ items, onClick }: { items: string[]; onClick: () => void }) => {
  return (
    <div>
      <h2>This is a List.</h2>
    </div>
  );
};

export default List;
